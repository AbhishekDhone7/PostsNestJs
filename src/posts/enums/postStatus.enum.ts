//draft, scheduled, review, published
/** Publication status values used by post persistence and API validation. */
export enum postStatus {
  DRAFT = 'draft',
  SCHEDULED = 'scheduled',
  REVIEW = 'review',
  PUBLISHED = 'published',
}
